import StatusBadge from "./StatusBadge";
const titleCase = (value) => value?.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
export default function ComplaintTable({ complaints, onHistory, onUpdate, loading }) {
  if (loading) return <div className="empty-state"><span className="spinner" /> Loading complaints…</div>;
  if (!complaints.length) return <div className="empty-state">No complaints match this view.</div>;
  return <div className="table-wrap"><table><thead><tr><th>ID</th><th>Issue</th><th>Category</th><th>Priority</th><th>Status</th><th>Assigned authority</th><th>Created</th>{onUpdate && <th>Action</th>}<th></th></tr></thead><tbody>{complaints.map((complaint) => <tr key={complaint.id}><td>#{complaint.id}</td><td><strong>{complaint.title}</strong><span className="table-description">{complaint.description}</span></td><td>{titleCase(complaint.category)}</td><td>{titleCase(complaint.priority)}</td><td><StatusBadge status={complaint.status} /></td><td>{complaint.assigned_authority_id ? `Authority #${complaint.assigned_authority_id}` : "Unassigned"}</td><td>{new Date(complaint.created_at).toLocaleDateString()}</td>{onUpdate && <td>{complaint.status === "assigned" && <button className="text-button" onClick={() => onUpdate(complaint.id, "in_progress")}>Start work</button>}{complaint.status === "in_progress" && <button className="text-button" onClick={() => onUpdate(complaint.id, "resolved")}>Resolve</button>}</td>}<td><button className="text-button" onClick={() => onHistory(complaint)}>History</button></td></tr>)}</tbody></table></div>;
}
