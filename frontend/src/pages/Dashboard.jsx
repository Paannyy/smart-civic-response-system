import { useCallback, useEffect, useState } from "react";
import { getAdminComplaints, getAssignedComplaints, getComplaintHistory, getMyComplaints, getUsers, updateComplaintStatus, updateUserStatus } from "../services/api";
import { useAuth } from "../context/AuthContext";
import ComplaintTable from "../components/ComplaintTable";
import StatusBadge from "../components/StatusBadge";
import CreateComplaint from "./CreateComplaint";

const blankFilters = { status: "", category: "", priority: "" };
const titleCase = (value) => value?.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
function Filters({ filters, setFilters }) { return <div className="filters"><select aria-label="Filter by status" value={filters.status} onChange={(e) => setFilters({ ...filters, status: e.target.value })}><option value="">All statuses</option><option value="pending">Pending</option><option value="assigned">Assigned</option><option value="in_progress">In progress</option><option value="resolved">Resolved</option></select><select aria-label="Filter by category" value={filters.category} onChange={(e) => setFilters({ ...filters, category: e.target.value })}><option value="">All categories</option><option value="garbage">Garbage</option><option value="water">Water</option><option value="electricity">Electricity</option><option value="roads">Roads</option></select><select aria-label="Filter by priority" value={filters.priority} onChange={(e) => setFilters({ ...filters, priority: e.target.value })}><option value="">All priorities</option><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option></select></div>; }

export default function Dashboard() {
  const { token, user, logout } = useAuth(); const [view, setView] = useState("complaints"); const [complaints, setComplaints] = useState([]); const [users, setUsers] = useState([]); const [filters, setFilters] = useState(blankFilters); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [history, setHistory] = useState(null); const [success, setSuccess] = useState("");
  const load = useCallback(async () => { setLoading(true); setError(""); try { if (user.role === "citizen") setComplaints(await getMyComplaints(token)); else if (user.role === "authority") setComplaints(await getAssignedComplaints(token, filters)); else { const [allComplaints, allUsers] = await Promise.all([getAdminComplaints(token, filters), getUsers(token)]); setComplaints(allComplaints); setUsers(allUsers); } } catch (err) { setError(err.message); } finally { setLoading(false); } }, [filters, token, user.role]);
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { if (view !== "create") load(); }, [load, view]);
  const showHistory = async (complaint) => { try { setHistory({ complaint, entries: null }); const entries = await getComplaintHistory(complaint.id, token); setHistory({ complaint, entries }); } catch (err) { setError(err.message); setHistory(null); } };
  const updateStatus = async (id, status) => { try { await updateComplaintStatus(id, status, token); setSuccess(`Complaint #${id} marked ${titleCase(status)}.`); load(); } catch (err) { setError(err.message); } };
  const toggleUser = async (target) => { const action = target.is_active ? "deactivate" : "activate"; if (!window.confirm(`Are you sure you want to ${action} ${target.name}?`)) return; try { await updateUserStatus(target.id, !target.is_active, token); setSuccess(`${target.name} has been ${action}d.`); load(); } catch (err) { setError(err.message); } };
  const citizen = user.role === "citizen"; const authority = user.role === "authority";
  return <main className="app-shell"><header className="topbar"><div className="brand"><span>SC</span><div>Smart Civic <small>Response System</small></div></div><div className="account"><span>{user.name}<small>{titleCase(user.role)} portal</small></span><button className="secondary" onClick={() => logout()}>Log out</button></div></header><div className="workspace"><aside className="sidebar"><p className="eyebrow">PORTAL</p><button className={view === "complaints" ? "nav-active" : ""} onClick={() => setView("complaints")}>{authority ? "Assigned complaints" : user.role === "admin" ? "All complaints" : "My complaints"}</button>{citizen && <button className={view === "create" ? "nav-active" : ""} onClick={() => setView("create")}>Create complaint</button>}{user.role === "admin" && <button className={view === "users" ? "nav-active" : ""} onClick={() => setView("users")}>User management</button>}</aside><section className="content"><div className="page-heading"><div><p className="eyebrow">{titleCase(user.role)} WORKSPACE</p><h1>{view === "create" ? "Create a complaint" : view === "users" ? "User management" : authority ? "Assigned complaints" : citizen ? "My complaints" : "Civic operations"}</h1><p className="muted">{authority ? "Manage issues assigned to your department." : citizen ? "Track every request you have submitted." : "Review system activity and manage user access."}</p></div>{view === "complaints" && <button className="secondary" onClick={load}>Refresh</button>}</div>{error && <p className="alert" role="alert">{error}</p>}{success && <p className="notice">{success}</p>}{view === "create" ? <CreateComplaint onCreated={(complaint) => { setSuccess(`Complaint #${complaint.id} submitted successfully.`); setView("complaints"); }} /> : view === "users" ? <section className="panel"><div className="section-heading"><h2>Users</h2></div>{loading ? <div className="empty-state"><span className="spinner" /> Loading users…</div> : <div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Department</th><th>Status</th><th></th></tr></thead><tbody>{users.map((target) => <tr key={target.id}><td><strong>{target.name}</strong></td><td>{target.email}</td><td>{titleCase(target.role)}</td><td>{target.department ? titleCase(target.department) : "—"}</td><td><span className={`user-status ${target.is_active ? "active" : "inactive"}`}>{target.is_active ? "Active" : "Inactive"}</span></td><td><button className="text-button" disabled={target.id === user.id} onClick={() => toggleUser(target)}>{target.is_active ? "Deactivate" : "Activate"}</button></td></tr>)}</tbody></table></div>}</section> : <><section className="metrics"><article><span>Total</span><strong>{complaints.length}</strong></article><article><span>Open</span><strong>{complaints.filter((item) => item.status !== "resolved").length}</strong></article><article><span>Resolved</span><strong>{complaints.filter((item) => item.status === "resolved").length}</strong></article></section>{!citizen && <Filters filters={filters} setFilters={setFilters} />}<section className="panel"><ComplaintTable complaints={complaints} loading={loading} onHistory={showHistory} onUpdate={authority ? updateStatus : null} /></section></>}</section></div>{history && <div className="modal-backdrop" role="presentation"><section className="modal" role="dialog" aria-modal="true" aria-label="Complaint history"><button className="modal-close" onClick={() => setHistory(null)} aria-label="Close history">×</button><p className="eyebrow">COMPLAINT #{history.complaint.id}</p><h2>{history.complaint.title}</h2><p className="muted">Status timeline</p>{history.entries === null ? <div className="empty-state"><span className="spinner" /> Loading history…</div> : history.entries.length ? <ol className="timeline">{history.entries.map((entry) => <li key={entry.id}><StatusBadge status={entry.status} /><span>{new Date(entry.created_at).toLocaleString()} · Updated by user #{entry.changed_by}</span></li>)}</ol> : <div className="empty-state">No status updates have been recorded yet.</div>}</section></div>}</main>;
}
